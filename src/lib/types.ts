// This file can be used for frontend-specific types that are not shared with the backend.
// For now, we will rely on shared/types.ts for our primary data models.
export type ExampleFrontendType = {
  uiSpecificProperty: string;
};